<%= form_for(venda) do |f| %>
  <% if venda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venda.errors.count, "error") %> prohibited this venda from being saved:</h2>

      <ul>
      <% venda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :valor %>
    <%= f.text_field :valor %>
  </div>

  <div class="field">
    <%= f.label :tipo_pagamento %>
    <%= f.text_field :tipo_pagamento %>
  </div>

  <div class="field">
    <%= f.label :locacao_id %>
    <%= f.number_field :locacao_id %>
  </div>

  <div class="actions">
    <%= f.button :submit, name: "time", value: Time.now %>
  </div>
<% end %>
